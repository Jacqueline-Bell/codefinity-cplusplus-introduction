#include <iostream>

int calculateReward(int customerPosition) {
    int reward = 100;
    for (int i = 1; i < customerPosition; i++) {
        reward += 50;
    }
    return reward;
}

int main() {
    int customerPosition = 15;
    int reward = calculateReward(customerPosition);

    std::cout << "The reward for the " << customerPosition 
              << "th customer is: " << reward << " points." << std::endl;
    return 0;
}